GROUP BY за колоните, които не участват в агрегираща функция!!!!
При InnoDB COUNT(*) и COUNT(1) ПРАВИ ЕДНО И СЪЩО!

При SELECT AVG(salary)....HAVING AVG(salary) -> AVG(salary) се изпълнява само веднъж!

ЗАЯВКА БЕЗ LIMIT НЕ СЕ ПУСКА КЪМ СЪРВЪРА!

WHERE сортира преди да си изпълни агрегиращата функция, а HAVING след това.